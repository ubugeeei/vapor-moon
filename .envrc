# direnv config — auto-loads the Nix devShell when entering the repo.
# Install direnv (https://direnv.net) then `direnv allow` once.

if command -v nix >/dev/null 2>&1; then
  use flake
else
  echo "[direnv] nix not installed — see README Quickstart for the manual setup."
fi
