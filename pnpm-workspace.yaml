packages:
  - "packages/*"
  - "examples/vite-app"
  - "e2e"
  - "editors/vscode"

# Centralize commonly-used dev dependencies so workspaces don't drift
# (typescript / vite / vitest / tsup versions are co-resolved).
catalog:
  typescript: "^5.6.0"
  vite: "^7.0.0"
  vitest: "^3.0.0"
  tsup: "^8.3.0"
  "@types/node": "^22.0.0"
