<script>
struct Props {
  title : String
  count : Int
}

struct Emits {
  save : String
  cancel : Unit
}

let props : Props = defineProps()
let emit : Emits = defineEmits()
</script>

<template>
  <button class="primary" @click='emit.save()'>
    {{ props.title }}
  </button>
</template>
